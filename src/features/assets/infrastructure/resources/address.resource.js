export class AddressResource {
    constructor({ street, number, district, city, country, postalCode }) {
        this.street = street;
        this.number = number;
        this.district = district;
        this.city = city;
        this.country = country;
        this.postalCode = postalCode;
    }
}
